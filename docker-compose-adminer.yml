version: '3.1'
services:
  adminer:
    image: adminer
    container_name: adminer_container
    ports:
      - "8080:8080"
    restart: always
    networks:
      - network

networks:
  network:
    external: true
